<Window
    x:Class="DataViewer.UI.Wpf.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  Ряд 0  -->
        <TextBlock Text="База" />

        <TextBlock
            Grid.Column="1"
            Text="Таблица" />

        <TextBlock
            Grid.Column="2"
            Text="Экземпляр" />

        <TextBlock
            Grid.Column="3"
            Text="Свойства" />

        <!--  Ряд 1  -->
        <TextBlock
            Grid.Row="1"
            Text="{Binding BindingTest}" />

    </Grid>
</Window>
