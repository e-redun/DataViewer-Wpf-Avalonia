<Window
    x:Class="DataViewer.UI.Wpf.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DataViewer (WPF)"
    Width="800"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="{StaticResource AllMargin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Ряд 0  -->
        <ContentPresenter Content="{Binding DataBasesVM}" />
        <ContentPresenter
            Grid.Column="1"
            Margin="{StaticResource LeftMargin}"
            Content="{Binding TablesVM}" />
        <ContentPresenter
            Grid.Column="2"
            Margin="{StaticResource LeftMargin}"
            Content="{Binding TableContentVM}" />
        <ContentPresenter
            Grid.Column="3"
            Margin="{StaticResource LeftMargin}"
            Content="{Binding PropertiesVM}" />

        <!--  Ряд 1  -->
        <Button
            Grid.Row="1"
            Grid.Column="3"
            Margin="{StaticResource TopMargin}"
            HorizontalAlignment="Right"
            Command="{Binding CloseApplicationCommand}"
            Content="Закрыть"
            IsCancel="True" />
    </Grid>
</Window>
