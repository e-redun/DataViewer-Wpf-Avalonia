<UserControl
    x:Class="DataViewer.UI.Wpf.Views.TablesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="clr-namespace:DataViewer.UI.Wpf.Views"
    d:DesignHeight="450"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <DockPanel>
        <TextBlock
            DockPanel.Dock="Top"
            Text="{Binding Header}" />

        <ListBox
            Margin="{StaticResource TopMargin}"
            Background="Transparent"
            ItemsSource="{Binding Tables}"
            SelectedItem="{Binding RelativeSource={RelativeSource AncestorType=v:MainWindow}, Path=DataContext.SelectedTable}" />

    </DockPanel>
</UserControl>
